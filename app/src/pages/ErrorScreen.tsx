import {Box} from "@mui/material";

export default function ErrorScreen() {
    return <Box sx={{style: "flex", width: "100vw", height: "100vh", justifyContent: "center", alignItems: "center"}}>
        Something went wrong
    </Box>
}